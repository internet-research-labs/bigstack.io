language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bigstack-io' ]; then cd .. && eval "mv $currentfolder generator-bigstack-io" && cd generator-bigstack-io; fi

